export const users = (state) => {
    return state.usersPage.users;
}

export const pageNumberUsers = (state) => {
    return state.usersPage.pageNumberUsers;
}

export const isPreloader = (state) => {
    return state.usersPage.isPreloader;
}

export const disebledProcces = (state) => {
    return state.usersPage.disebledProcces;
}